apiVersion: v1
kind: Service
metadata:
  name: siof-storage-headless
  namespace: {{ .Values.namespace }}
spec:
  clusterIP: None
  selector:
    app: siof-storage
  ports:
    - name: bolt
      port: 7687
      targetPort: 7687
